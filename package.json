{
  "name": "coorward-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && rm -fr docs && next export && touch out/.nojekyll && echo \"coorward.mashapp.xyz\" > out/CNAME",
    "start": "next start",
    "deploy": "yarn build && git add out/ && git commit -m \"Deploy to gh-pages\"  && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "@blueprintjs/core": "^3.26.0",
    "next": "9.3.6",
    "react": "16.13.1",
    "react-copy-to-clipboard": "^5.0.2",
    "react-dom": "16.13.1",
    "styled-components": "^5.1.0",
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "@types/node": "^13.13.4",
    "@types/react": "^16.9.34"
  }
}
